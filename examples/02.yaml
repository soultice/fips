- PROXY: 
    path: ^/foo/$
    forwardUri:
      "http://localhost:8888/mock"
    backwardHeaders:
      - "x-app-name"

- PROXY: 
    path: ^/foo/$
    forwardUri:
      "http://localhost:8888/mock"
